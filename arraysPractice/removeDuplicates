let array = ["Hare", "Krishna", "Hare", "Krishna", "Krishna", ":-O"];

function unique(array) {
  let result = [];

  for (let str of array) {
    if (!result.includes(str)) {
      result.push(str);
    }
  }

  return result;
}

console.log(unique(array));
